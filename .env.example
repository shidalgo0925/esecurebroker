# Development default: local SQLite (own DB file — not EN1/IIUS).
# Production/staging: set PostgreSQL URL for dedicated corredores database.
DATABASE_URL=sqlite:////opt/corredores/var/corredores_p0.db
APP_ENV=dev
LOG_LEVEL=INFO
