# ESecureBroker — esecurebroker.etsrv.site → uvicorn 127.0.0.1:8091
# DNS A (origin): 86.48.20.243 · SSL: Let's Encrypt

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name esecurebroker.etsrv.site;

    include /etc/nginx/snippets/cloudflare-realip.conf;

    ssl_certificate /etc/letsencrypt/live/esecurebroker.etsrv.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/esecurebroker.etsrv.site/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 32M;

    location / {
        proxy_pass http://127.0.0.1:8091;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name esecurebroker.etsrv.site;

    include /etc/nginx/snippets/cloudflare-realip.conf;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
